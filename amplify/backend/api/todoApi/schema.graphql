type Todo
  @model
  @auth(
    rules: [
      { allow: owner, ownerField: "owner" }
      { allow: groups, groups: ["Admin"] }
      { allow: groups, groupsField: "group" }
    ]
  ) {
  id: ID!
  name: String!
  description: String
  owner: String
  group: String
}
